CLASS net/minecraft/network/Varint21LengthFieldPrepender
	COMMENT Prepends each frame ("packet") with its length encoded as a VarInt. Every frame's length must fit within a 3-byte VarInt.
	COMMENT
	COMMENT @see Varint21FrameDecoder
